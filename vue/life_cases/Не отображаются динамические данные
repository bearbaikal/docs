Важно!!! Когда я в функции меняю данные, например, с помощью foreach,
то важно менять сначала не ту переменную, которую я вывожу в браузер с помощью,
например, {{}} или v-for, а менять отдельную переменную и когда она уже будет готова,
то только тогда её присваивать тому, что будет в браузере.

Vue не сможет обработать корректно изменение в цикле большого количества данных
и просто не выведет в браузер ничего, либо может вывести, но поведение будет непредсказуемо.
(Пример: Случай с выводом истории тачек в ЛК, когда история группировалась по месяцам в foreach.
         Тогда страница либо не подгружала данные вовсе, либо подгружала, но ошибки не было)